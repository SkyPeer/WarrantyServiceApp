'use strict';

exports.__esModule = true;
var parse = function parse(cause) {
  var error = cause.error;
  var message = cause.message;

  if (error) {
    return error;
  } else if (message) {
    return message;
  }
  return '';
};

var newError = function newError(cause) {
  var e = new Error(parse(cause));
  e.cause = cause;
  return e;
};

exports.default = newError;